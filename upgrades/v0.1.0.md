# v0.1.0

本仓首个工牌发版：登记 `evo_agent_pack.json`，可按 tag clone 复现本仓。

## 变更

- 根目录 `evo_agent_pack.json`（schema_version 1）：`name=e_a_t_ai-auto-slice-edit`，`dirname=AI自动化切片剪辑拉片`，`version=0.1.0`，`dependencies={}`（兄弟仅 `learns_from`，不进合同）
- 脚手架 scaffold_version 2.3.0；业务：脚本解析、高光时间轴、按脚本拼接初剪
- 峰会高光手法工作流：`choose-edit-style` → `summit-highlight`（编排 dualcam + jinju）；机位随用户片源
- 发版约定：`upgrades/` 说明 + `docs/builtin-workflows/cut-release-tag.md`

## 升级注意

- clone / 切换到本 tag：`git clone --branch v0.1.0 https://github.com/0x00pluto/evo_agent_team_ai-auto-slice-edit.git`
- 核对根目录 `evo_agent_pack.json` 的 `version` 为 `0.1.0`（不含 v）
- 本仓不提供便携包 zip；拉源码后用本仓 `.venv` + UV 按 `AGENTS.md` 运行
